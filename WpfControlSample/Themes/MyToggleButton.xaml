<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:local="clr-namespace:WpfControlSample.Converters">
    <local:SubtractConverter x:Key="SubtractConverter"/>
    <local:PercentageCornerRadiusConverter x:Key="CornerConverter" />

    <DropShadowEffect x:Key="TrackInnerShadow"
                      Color="#000000"
                      BlurRadius="6"
                      ShadowDepth="0"
                      Opacity="0.3"
                      RenderingBias="Performance" />

    <DropShadowEffect x:Key="ThumbOuterShadow"
                      Color="#000000"
                      BlurRadius="4"
                      ShadowDepth="2"
                      Opacity="0.2"
                      Direction="270"
                      RenderingBias="Performance" />


    <Style x:Key="MyToggleButton" TargetType="ToggleButton">
        <Setter Property="MinWidth" Value="50"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Background" Value="WhiteSmoke"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <!-- ① ルート Grid に TemplateBinding -->
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <Border 
                            x:Name="Track"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{Binding Height,
                                            RelativeSource={RelativeSource Mode=TemplatedParent},
                                            Converter={StaticResource CornerConverter},
                                            ConverterParameter=50}"           
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Effect="{StaticResource TrackInnerShadow}"/>
                        <Ellipse
                            x:Name="Thumb" 
                            Width="{Binding Height, 
                                    RelativeSource={RelativeSource Mode=TemplatedParent},
                                    Converter={StaticResource SubtractConverter},
                                    ConverterParameter=2}" 
                            Height="{Binding Height, 
                                    RelativeSource={RelativeSource Mode=TemplatedParent},
                                    Converter={StaticResource SubtractConverter},
                                    ConverterParameter=2}"
                            Fill="DarkCyan"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Effect="{StaticResource ThumbOuterShadow}">
                            <Ellipse.RenderTransform>
                                <TranslateTransform x:Name="ThumbTransform" X="2"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>

                    <!-- トリガー・アニメは従来通り -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                      Storyboard.TargetName="Track"
                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                      To="LimeGreen"
                      Duration="0:0:0.2"/>
                                        <!-- 固定値だった X=24 は省いて、理想的には (ActualWidth–ActualHeight–マージン) で計算 -->
                                        <DoubleAnimation
                      Storyboard.TargetName="ThumbTransform"
                      Storyboard.TargetProperty="X"
                      To="24"
                      Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                      Storyboard.TargetName="Track"
                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                      To="#CCC"
                      Duration="0:0:0.2"/>
                                        <DoubleAnimation
                      Storyboard.TargetName="ThumbTransform"
                      Storyboard.TargetProperty="X"
                      To="2"
                      Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Track" Property="Opacity" Value="0.6"/>
                            <Setter TargetName="Thumb" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>